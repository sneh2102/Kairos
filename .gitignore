venv/
__pycache__/
*.pyc
.env
*.db
outputs/
config.json
projects.txt
resume.txt
templates/
previews/

# PyInstaller freeze + bundled Chromium/Tectonic — built artifacts, not source
/build_backend/
/dist_backend/
/desktop/release/

build
dist/
Job-scraper-LangGraph
ziprecruiter_debug.png
# Bundled native binaries are fetched per-OS in CI, not committed.
# resume-icons are small platform-agnostic source assets — keep them tracked.
desktop/build-resources/*
!desktop/build-resources/resume-icons/